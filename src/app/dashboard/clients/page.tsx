import ComingSoon from "@/components/ComingSoon";

export const dynamic = "force-dynamic";

export default function ClientsPage() {
  return (
    <ComingSoon
      title="Clients"
      description="Manage your clients in one place — billing details, contact info, and invoice history."
      eta="Phase 2"
    />
  );
}
